# 3. 두 실수를 배열에 초기화해서 저장하고 더한 후 화면에 출력 하는 C프로그램을 작성하시오.

```
#include <stdio.h>
int main() {
int a[2] = { 2, 3 };
int sum;
sum = a[0] + a[1];
printf("%d + %d =", a[0], a[1]);
    printf(" %d", sum);

    return 0;
}
```
