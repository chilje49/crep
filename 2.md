# 2. 두 실수를 키보드에서 읽고 메모리에 저장한 후 더한 후 화면에 출력하는 C프로그램을 작성하시오.

```
#include<stdio.h>
int main() {
	double a, b, c;
	scanf_s("%lf", &a);
	scanf_s("%lf", &b);
	c = a + b;
	printf_s("%lf", c);
}
```
### 입력한 값 : a = 5.5, b = 4.4
### 출력된 값 : 9.900000
